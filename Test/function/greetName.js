function greetName(name, language){
if(language === 'English'){
  return 'Hello ' + name;
  }

  else if(language === 'isiXhosa'){
    return 'Molo ' + name;
  }

  else if(language === 'Sotho'){
    return 'Dumela ' + name;
  }
}
